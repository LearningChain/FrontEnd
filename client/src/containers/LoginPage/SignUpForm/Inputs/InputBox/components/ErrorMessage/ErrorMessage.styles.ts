import styled from 'styled-components';
import colors from '@/_shared/colors/colors'

export const MessageContainer = styled.div`
  width: 100%;
`;

export const Message = styled.p`
  font-size: 12px;
  color: ${colors.errorOpacity100};
`;